New info:

Middleware can end the HTTPS request by sending back a response with methos like res.send()

Middlewares can be chained together, one after another by calling next() 

For logs we use:
- morgan
    